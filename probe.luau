--!strict
-- Generated by Zypher In-Engine Probe Packager
-- (c) 2026 ZypherDev / MSayib

local Probe = {}

Probe.Sources = {
    ["m01_mirror_arith"] = "local function mi_plus(p, q) return q + p end\nlocal function mi_minus(p, q) return q - p end\nlocal function mi_times(p, q) return q * p end\nlocal function mi_over(p, q) return q / p end\nlocal function mi_rem(p, q) return q % p end\nlocal function mi_raise(p, q) return q ^ p end\nlocal function mi_floordiv(p, q) return q // p end\nlocal function mi_plusk(p) return p + 11 end\nlocal function mi_minusk(p) return p - 11 end\nlocal function mi_timesk(p) return p * 11 end\nlocal function mi_overk(p) return p / 11 end\nlocal function mi_remk(p) return p % 11 end\nlocal function mi_raisek(p) return p ^ 11 end\nlocal function mi_floork(p) return p // 11 end\nlocal function mi_rminus(p) return 11 - p end\nlocal function mi_rover(p) return 11 / p end\nlocal function mi_negate(p) return -p end\nlocal function mi_invert(p) return not p end\nlocal function mi_count(p) return #p end\nlocal function mi_join(p, q) return p .. q .. p end\nlocal function mi_yes() local v = true return v end\nlocal function mi_no() local v = false return v end\nlocal function mi_none() local v = nil return v end\nlocal function mi_small() local v = 12 return v end\nlocal function mi_text() local v = \"mirror constant string\" return v end\nreturn {\n\tmi_plus, mi_minus, mi_times, mi_over, mi_rem, mi_raise, mi_floordiv,\n\tmi_plusk, mi_minusk, mi_timesk, mi_overk, mi_remk, mi_raisek, mi_floork,\n\tmi_rminus, mi_rover, mi_negate, mi_invert, mi_count, mi_join,\n\tmi_yes, mi_no, mi_none, mi_small, mi_text,\n}\n",
    ["m02_mirror_branch"] = "local function mb_below(p, q, box) if p < q then box.flag = 0 end return box end\nlocal function mb_notbelow(p, q, box) if not (p < q) then box.flag = 0 end return box end\nlocal function mb_atmost(p, q, box) if p <= q then box.flag = 0 end return box end\nlocal function mb_notatmost(p, q, box) if not (p <= q) then box.flag = 0 end return box end\nlocal function mb_same(p, q, box) if p == q then box.flag = 0 end return box end\nlocal function mb_differ(p, q, box) if p ~= q then box.flag = 0 end return box end\nlocal function mb_truthy(p, box) if p then box.flag = 0 end return box end\nlocal function mb_falsy(p, box) if not p then box.flag = 0 end return box end\nlocal function mb_isnil(p, box) if p == nil then box.flag = 0 end return box end\nlocal function mb_istrue(p, box) if p == true then box.flag = 0 end return box end\nlocal function mb_isnum(p, box) if p == 19 then box.flag = 0 end return box end\nlocal function mb_isstr(p, box) if p == \"mirror comparison literal\" then box.flag = 0 end return box end\nlocal function mb_repeat(p, box) repeat box.flag = box.flag + 1 until box.flag > p return box end\nlocal function mb_split(p, box)\n\tif p then box.a = 0 else box.a = 1 end\n\tbox.b = 2\n\tbox.c = 3\n\tbox.d = 4\n\treturn box\nend\nreturn {\n\tmb_below, mb_notbelow, mb_atmost, mb_notatmost, mb_same, mb_differ,\n\tmb_truthy, mb_falsy, mb_isnil, mb_istrue, mb_isnum, mb_isstr,\n\tmb_repeat, mb_split,\n}\n",
    ["m03_mirror_table"] = "local function mt_empty(k) local box = {} box[k] = k return box end\nlocal function mt_named() return { first = 4, second = 5 } end\nlocal function mt_array(p, q, r) return { p, q, r } end\nlocal function mt_field(box) box.left = box.right return box end\nlocal function mt_index(box, k) return box[k] end\nlocal function mt_slot(box) box[5] = box[4] return box end\nlocal function mt_both(p) return p and 6 end\nlocal function mt_either(p) return p or 6 end\nlocal function mt_andreg(p, q) return p and q end\nlocal function mt_orreg(p, q) return p or q end\nreturn { mt_empty, mt_named, mt_array, mt_field, mt_index, mt_slot, mt_both, mt_either, mt_andreg, mt_orreg }\n",
    ["m04_mirror_flow"] = "local function mf_count(limit, acc) for step = 1, limit do acc = acc + step end return acc end\nlocal function mf_seq(box, acc) for idx in ipairs(box) do acc = acc + idx end return acc end\nlocal function mf_map(box, acc) for key in pairs(box) do acc = acc + 1 end return acc end\nlocal function mf_raw(box, acc) for key in next, box do acc = acc + 1 end return acc end\nlocal function mf_custom(box, acc) for one in box.walk, box, nil do acc = acc + 1 end return acc end\nlocal function mf_bind()\n\tlocal total = 0\n\treturn function() total = total + 1 return total end\nend\nlocal function mf_each(box)\n\tfor idx = 1, 3 do\n\t\tlocal held = idx\n\t\tbox[idx] = function() return held end\n\tend\n\treturn box\nend\nlocal function mf_free() return function() return 0 end end\nlocal function mf_apply(fn, one) return fn(one) end\nlocal function mf_send(obj, one) return obj:mirrorMethod(one) end\nlocal function mf_spread(...) local one, two = ... return one, two end\nlocal function mf_collect(...) return { ... } end\nlocal function mf_relay(fn, ...) return fn(...) end\nlocal function mf_abs(one) return math.abs(one) end\nlocal function mf_max(one, two) return math.max(one, two) end\nlocal function mf_slice(text) return string.sub(text, 3) end\nlocal function mf_clamp(one, two, three) return math.clamp(one, two, three) end\nlocal function mf_widest(...) return math.max(...) end\nlocal function mf_store(v) MirrorGlobalSlot = v end\nlocal function mf_load() return MirrorGlobalSlot end\nreturn {\n\tmf_count, mf_seq, mf_map, mf_raw, mf_custom, mf_bind, mf_each, mf_free,\n\tmf_apply, mf_send, mf_spread, mf_collect, mf_relay,\n\tmf_abs, mf_max, mf_slice, mf_clamp, mf_widest, mf_store, mf_load,\n}\n",
    ["p01_arith"] = "local function ar_add(a, b) return a + b end\nlocal function ar_sub(a, b) return a - b end\nlocal function ar_mul(a, b) return a * b end\nlocal function ar_div(a, b) return a / b end\nlocal function ar_mod(a, b) return a % b end\nlocal function ar_pow(a, b) return a ^ b end\nlocal function ar_idiv(a, b) return a // b end\nreturn { ar_add, ar_sub, ar_mul, ar_div, ar_mod, ar_pow, ar_idiv }\n",
    ["p02_arithk"] = "local function kk_add(a) return a + 3 end\nlocal function kk_sub(a) return a - 3 end\nlocal function kk_mul(a) return a * 3 end\nlocal function kk_div(a) return a / 3 end\nlocal function kk_mod(a) return a % 3 end\nlocal function kk_pow(a) return a ^ 3 end\nlocal function kk_idiv(a) return a // 3 end\nlocal function rk_sub(a) return 3 - a end\nlocal function rk_div(a) return 3 / a end\nreturn { kk_add, kk_sub, kk_mul, kk_div, kk_mod, kk_pow, kk_idiv, rk_sub, rk_div }\n",
    ["p03_unary"] = "local function un_not(x) return not x end\nlocal function un_minus(x) return -x end\nlocal function un_len(x) return #x end\nreturn { un_not, un_minus, un_len }\n",
    ["p04_load"] = "local function ld_nil() local a = nil return a end\nlocal function ld_true() local a = true return a end\nlocal function ld_false() local a = false return a end\nlocal function ld_num() local a = 7 return a end\nlocal function ld_str() local a = \"probe constant string one\" return a end\nlocal function ld_move(x) local a = x local b = a return b end\nlocal function cc_three(a, b, c) return a .. b .. c end\nreturn { ld_nil, ld_true, ld_false, ld_num, ld_str, ld_move, cc_three }\n",
    ["p05_global"] = "local function gl_set(v) ProbeGlobalSlot = v end\nlocal function gl_get() return ProbeGlobalSlot end\nreturn { gl_set, gl_get }\n",
    ["p06_branch"] = "local function br_lt(a, b, s) if a < b then s.n = 1 end return s end\nlocal function br_nlt(a, b, s) if not (a < b) then s.n = 1 end return s end\nlocal function br_le(a, b, s) if a <= b then s.n = 1 end return s end\nlocal function br_nle(a, b, s) if not (a <= b) then s.n = 1 end return s end\nlocal function br_eq(a, b, s) if a == b then s.n = 1 end return s end\nlocal function br_neq(a, b, s) if a ~= b then s.n = 1 end return s end\nlocal function br_if(a, s) if a then s.n = 1 end return s end\nlocal function br_ifnot(a, s) if not a then s.n = 1 end return s end\nlocal function br_while(a, s) while s.n < a do s.n = s.n + 1 end return s end\nlocal function br_jump(a, s)\n\tif a then s.x = 1 else s.x = 2 end\n\ts.p = 1\n\ts.q = 2\n\ts.r = 3\n\treturn s\nend\nreturn { br_lt, br_nlt, br_le, br_nle, br_eq, br_neq, br_if, br_ifnot, br_while, br_jump }\n",
    ["p07_xeq"] = "local function xq_nil(a, s) if a == nil then s.n = 1 end return s end\nlocal function xq_bool(a, s) if a == true then s.n = 1 end return s end\nlocal function xq_num(a, s) if a == 7 then s.n = 1 end return s end\nlocal function xq_str(a, s) if a == \"probe equality string\" then s.n = 1 end return s end\nreturn { xq_nil, xq_bool, xq_num, xq_str }\n",
    ["p08_logic"] = "local function lg_and(a, b) return a and b end\nlocal function lg_or(a, b) return a or b end\nlocal function lg_andk(a) return a and 5 end\nlocal function lg_ork(a) return a or 5 end\nreturn { lg_and, lg_or, lg_andk, lg_ork }\n",
    ["p09_table"] = "local function tb_new(x) local t = {} t[x] = x return t end\nlocal function tb_dup() return { alpha = 1, beta = 2 } end\nlocal function tb_list(a, b, c) return { a, b, c } end\nlocal function tb_ks(t) t.field = t.other return t end\nlocal function tb_get(t, k) return t[k] end\nlocal function tb_num(t) t[3] = t[2] return t end\nreturn { tb_new, tb_dup, tb_list, tb_ks, tb_get, tb_num }\n",
    ["p10_loop"] = "local function lp_num(n, s) for i = 1, n do s = s + i end return s end\nlocal function lp_ipairs(t, s) for i in ipairs(t) do s = s + i end return s end\nlocal function lp_pairs(t, s) for k in pairs(t) do s = s + 1 end return s end\nlocal function lp_next(t, s) for k in next, t do s = s + 1 end return s end\nlocal function lp_gen(t, s) for a in t.iter, t, nil do s = s + 1 end return s end\nreturn { lp_num, lp_ipairs, lp_pairs, lp_next, lp_gen }\n",
    ["p11_closure"] = "local function cl_counter()\n\tlocal n = 0\n\treturn function() n = n + 1 return n end\nend\nlocal function cl_scoped(t)\n\tfor i = 1, 3 do\n\t\tlocal v = i\n\t\tt[i] = function() return v end\n\tend\n\treturn t\nend\nlocal function cl_plain()\n\treturn function() return 1 end\nend\nreturn { cl_counter, cl_scoped, cl_plain }\n",
    ["p12_call"] = "local function ca_call(f, a) return f(a) end\nlocal function ca_method(o, a) return o:probeMethod(a) end\nlocal function ca_vararg(...) local a, b = ... return a, b end\nlocal function ca_pack(...) return { ... } end\nlocal function ca_fwd(f, ...) return f(...) end\nreturn { ca_call, ca_method, ca_vararg, ca_pack, ca_fwd }\n",
    ["p13_fastcall"] = "local function fc_one(a) return math.abs(a) end\nlocal function fc_two(a, b) return math.max(a, b) end\nlocal function fc_twok(s) return string.sub(s, 2) end\nlocal function fc_three(a, b, c) return math.clamp(a, b, c) end\nlocal function fc_multi(...) return math.max(...) end\nreturn { fc_one, fc_two, fc_twok, fc_three, fc_multi }\n",
}

function Probe.Run(compileFunc: (string) -> string?): { [string]: string }
    local results = {}
    for name, source in pairs(Probe.Sources) do
        local ok, bytecode = pcall(compileFunc, source)
        if ok and bytecode then
            results[name] = bytecode
        end
    end
    return results
end

return Probe
